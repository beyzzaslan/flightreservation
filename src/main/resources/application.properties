spring.application.name=flightreservation

# Veritabanı Bağlantısı (Docker'daki bilgilerle aynı olmalı)
spring.datasource.url=jdbc:postgresql://localhost:5432/flightreservation
spring.datasource.username=postgres
spring.datasource.password=password

# Hibernate Ayarları (Tabloları otomatik oluşturması için)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true